[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "libsodium",
  "gcc"
]

[phases.install]
cmds = [
  "python3 -m pip install --break-system-packages -r misc/requirements.txt"
]

[start]
cmd = "python3 -m bot.main"